export type CatalogFilters = {
  location?: string;
  form?: string;
  transmission?: string;
  engine?: string;
};

export type FilterOptions = {
  locations: string[];
  forms: string[];
  transmissions: string[];
  engines: string[];
};
